(async()=>{const button=document.querySelector('#uploadBtn'),input=document.querySelector('#fileInput'),status=document.querySelector('#status');button?.addEventListener('click',async()=>{const file=input.files[0];if(!file)return status.textContent='Choose an IFC, IFCZIP, or XLSX file.';if(!/\.(ifc|ifczip|xlsx)$/i.test(file.name))return status.textContent='Unsupported file type.';status.textContent='Creating secure temporary session…';const session=await fetch('/api/session',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'}).then(r=>r.json());const form=new FormData();form.append('files',file);status.textContent='Uploading for temporary processing…';const response=await fetch(`/api/session/${session.session_id}/upload`,{method:'POST',body:form});status.textContent=response.ok?'Upload complete. Validation workflow is ready to process this file.':'Upload failed safely. Please check the file and try again.';});})();
